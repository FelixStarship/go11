apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temporal.fullname" . }}-test-connection"
  labels:
    {{- include "temporal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "temporal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
